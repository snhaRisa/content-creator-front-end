Bugs & Fixes;
    1.) Home Page doesn't makes any API call to get the user. Need to implement that.   FIXED


Payment Integration; 
Will let user create only one plan or two plans. He would not be able to create more than 2 plans. 
Then the viewer would have to select that plan and pay that amount. 
Changes to form, 
if already made 2 plans, not let them create any more. 
